`socko.factory.img` is the stock P22H190 socko filesystem used as the
template. The build script replaces the selected kernel modules after
updating their symbol CRCs and vermagic for the newly built kernel.
